<%= form_for @subscriber do |f| %>
  <%= f.email_field :email, placeholder: 'Email' %>
    <% Subscriber::PREFERENCES.each do |key| %>
      <%= f.check_box :preferences, { multiple: true}, key, nil %>
      <%= f.label key%>
    <% end %>
  <%= f.submit %>
<% end %>